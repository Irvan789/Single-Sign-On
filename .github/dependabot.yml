version: 2

updates:
    - package-ecosystem: "composer"
      directory: "/"
      schedule:
          interval: "friday"

    - package-ecosystem: "bun"
      directory: "/"
      schedule:
          interval: "friday"
